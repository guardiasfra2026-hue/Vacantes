import Link from 'next/link';import {vacancies} from '@/data/mockData';import {Header} from '@/components/Header';import {Footer} from '@/components/Footer';import {VacancyCard} from '@/components/VacancyCard';
export default function State({params}:{params:{estado:string}}){const name=params.estado.replaceAll('-',' ');const list=vacancies.filter(v=>v.state.toLowerCase()===name.toLowerCase());return <><Header/><main className="mx-auto max-w-7xl px-4 py-12 md:px-8"><Link href="/" className="font-bold underline decoration-[#8BE40C]">← Inicio</Link><h1 className="mt-5 text-4xl font-black">Vacantes en {list[0]?.state||name}</h1><div className="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{list.map(v=><VacancyCard key={v.id} v={v}/>)}</div></main><Footer/></>}