import Link from 'next/link';import {notFound} from 'next/navigation';import {articles} from '@/data/mockData';import {Header} from '@/components/Header';import {Footer} from '@/components/Footer';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))};export default function Article({params}:{params:{slug:string}}){const a=articles.find(x=>x.slug===params.slug);if(!a)notFound();return <><Header/><main className="mx-auto max-w-3xl px-4 py-12 md:px-8"><Link href="/noticias" className="font-bold underline decoration-[#8BE40C]">← Volver al blog</Link><article className="mt-6 rounded-3xl bg-white p-6 shadow-soft md:p-10"><span className="text-sm font-bold uppercase text-gray-500">{a.category}</span><h1 className="mt-3 text-4xl font-black">{a.title}</h1><p className="mt-6 text-lg leading-8 text-gray-600">{a.text}</p><p className="mt-6 text-gray-600">Encuentra más recomendaciones y oportunidades en nuestro portal. Mantén actualizado tu CV, conoce tus fortalezas y postúlate a las vacantes que mejor coincidan contigo.</p></article></main><Footer/></>}