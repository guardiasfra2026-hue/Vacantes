import Link from 'next/link';import {notFound} from 'next/navigation';import {vacancies} from '@/data/mockData';import {Header} from '@/components/Header';import {Footer} from '@/components/Footer';
export function generateStaticParams(){return vacancies.map(v=>({id:v.id}))};export default function Detail({params}:{params:{id:string}}){const v=vacancies.find(x=>x.id===params.id);if(!v)notFound();return <><Header/><main className="mx-auto max-w-4xl px-4 py-12 md:px-8"><Link href="/vacantes" className="font-bold underline decoration-[#8BE40C]">← Volver a vacantes</Link><article className="mt-6 rounded-3xl border border-black/10 bg-white p-6 shadow-soft md:p-10"><span className="rounded-full bg-[#8BE40C] px-3 py-1 text-sm font-bold">{v.modality}</span><h1 className="mt-5 text-4xl font-black">{v.title}</h1><p className="mt-3 text-lg text-gray-600">{v.company} · {v.city}, {v.state}</p><p className="mt-2 font-bold">{v.salary}</p><section className="mt-10"><h2 className="text-2xl font-black">Descripción</h2><p className="mt-3 text-gray-600">{v.description}</p></section><section className="mt-8"><h2 className="text-2xl font-black">Requisitos</h2><ul className="mt-3 list-disc space-y-2 pl-6 text-gray-600">{v.requirements.map(r=><li key={r}>{r}</li>)}</ul></section><Link href={`/postular/${v.id}`} className="mt-10 inline-block rounded-full bg-[#8BE40C] px-6 py-3 font-black hover:bg-[#8BDD1F]">Postularme</Link></article></main><Footer/></>}